# Frontend Production Environment — Render Static Site
# These are baked into the JS bundle at build time (VITE_ prefix)

# API calls go through Render's static site rewrite rules (proxied to backend)
# No need to hardcode backend URL — the rewrite handles /api/* -> backend
VITE_API_URL=/api
