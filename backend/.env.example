# Django
SECRET_KEY=generate-a-long-random-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL is required for local development and production.
DATABASE_ENGINE=postgresql
DB_NAME=sahara_gold
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# URLs
FRONTEND_URL=http://localhost:5173
BACKEND_URL=http://127.0.0.1:8000
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# Production: set DEBUG=False, use HTTPS URLs, and explicit hostnames.
# Production also requires a non-sandbox SSLCommerz account.

# SSLCommerz Payment Gateway
SSLCOMMERZ_STORE_ID=replace-with-real-store-id
SSLCOMMERZ_STORE_PASS=replace-with-real-store-password
SSLCOMMERZ_IS_SANDBOX=True

# Email
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=
STORE_EMAIL=

# AI
HUGGINGFACE_API_KEY=
AI_MODEL=Qwen/Qwen2.5-72B-Instruct
STORE_NAME=Sahara Gold & Diamond
STORE_PHONE=
STORE_WHATSAPP=
STORE_ADDRESS=
WEBHOOK_VERIFY_TOKEN=
META_APP_SECRET=
